import { ModulePlaceholder } from '@/components/app/ModulePlaceholder'

export default function AchatsPage() {
  return (
    <ModulePlaceholder
      title="Bons de commande"
      description="Les achats ne sont pas encore relies aux tables `purchase_orders` et `purchase_order_lines`. La page de demonstration a ete retiree pour eviter tout faux flux."
    />
  )
}
