import { ModulePlaceholder } from '@/components/app/ModulePlaceholder'

export default function CommandesPage() {
  return (
    <ModulePlaceholder
      title="Commandes"
      description="Les commandes ne sont pas encore branchees sur `sales_orders` et `sales_order_lines`. Les donnees de demonstration ont ete supprimees et aucun bouton n'est laisse actif tant que le flux reel n'est pas disponible."
    />
  )
}
